server:
  port: 9001

spring:
  application:
    name: highloaded-microservice
  cloud:
#   to use configs from config server it should be launched first
    config:
      uri: http://localhost:8888
      fail-fast: true