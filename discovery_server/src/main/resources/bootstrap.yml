###bootstrap configurations of discovery server
#All other properties should be got from config server according to configuration management pattern

spring:
  application:
    name: discovery_server
#   to use configs from config server it should be launched first
  cloud:
    config:
      uri: http://localhost:8888    #config server uri
      fail-fast: true               #fail if properties could not be fetched